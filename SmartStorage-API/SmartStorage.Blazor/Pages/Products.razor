@page "/product"

@inject ApiExtensions api

<Alert></Alert>

@code {

    protected override async Task OnInitializedAsync()
    {
        var newProduct = new ProductVO
        {
            Name = "TesteAPIExtension",
            Descricao = "Test",
            DateRegister = DateTime.Now,
            Qntd = 10,
            EmployeeId = 1
        };

       var result = await api.Post<ProductVO>("api/storage/products/v1", newProduct);
    }

}